interface Colors {
  base: object;
  functional: object;
  neutral: object;
}

const base = {
  primary: "#fff",
};

const functional = {
  primary: "#fff",
};

const neutral = {
  primary: "#fff",
};

export const colors: Colors = {
  base,
  functional,
  neutral,
};
